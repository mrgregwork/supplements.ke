---
import BaseLayout from '@layouts/BaseLayout.astro';
import ProductCard from '@components/ProductCard.astro';
import siteSettings from '@config/siteSettings.json';
import { generateHomePageSchema, generateItemListSchema } from '@lib/schema';
import { storage } from '@lib/storage';

const heroData = await storage.getHomepageSection('hero');
const seoData  = await storage.getHomepageSection('seo');
const hero       = (heroData?.content || {}) as Record<string, any>;
const seoContent = (seoData?.content  || {}) as Record<string, any>;

// Live data
const allCategories = await storage.getCategories();
const activeCategories = allCategories.filter(c => c.isActive);
const allProducts = await storage.getProducts();
const activeProducts = allProducts.filter(p => p.status === 'active');

// Bestsellers row: featured first, then newest. Cap 10 (carousel scrolls).
const featured = activeProducts.filter(p => p.featured);
const bestsellers = (featured.length >= 5 ? featured : [...featured, ...activeProducts.filter(p => !p.featured)]).slice(0, 10);

// "Find your supplements" — pick 5 supplement-type categories with most products
async function categoryWithProducts(slug: string, limit = 6) {
  const cat = activeCategories.find(c => c.slug === slug);
  if (!cat) return null;
  const products = await storage.getProductsByCategory(cat.id);
  if (!products.length) return null;
  return { ...cat, products: products.slice(0, limit) };
}
const supplementTypes = (await Promise.all([
  categoryWithProducts('protein'),
  categoryWithProducts('collagen'),
  categoryWithProducts('vitamins'),
  categoryWithProducts('minerals'),
  categoryWithProducts('omega-fatty-acids'),
])).filter(Boolean) as Array<NonNullable<Awaited<ReturnType<typeof categoryWithProducts>>>>;

// "Shop by Goal" — pick 4 goal-themed categories with hero imagery (or fall back)
const GOAL_TILES = [
  { slug: 'protein',         label: 'Build Lean Muscle',     blurb: 'Protein, creatine, and recovery formulas' },
  { slug: 'specialty-supplements', label: 'Improve Performance',   blurb: 'Pre-workout, energy, and endurance' },
  { slug: 'health-goals',    label: 'Support Health & Longevity', blurb: 'Daily essentials for whole-body wellness' },
  { slug: 'weight-management', label: 'Body Recomposition',    blurb: 'Fat-loss, lean-mass, and metabolism support' },
];
const goalTiles = GOAL_TILES.map(t => ({ ...t, cat: activeCategories.find(c => c.slug === t.slug) })).filter(t => t.cat);

// Brand strip — top brands by product count
const brandCounts = new Map<string, number>();
activeProducts.forEach(p => { if (p.brand) brandCounts.set(p.brand, (brandCounts.get(p.brand) || 0) + 1); });
const topBrands = Array.from(brandCounts.entries()).sort((a,b) => b[1]-a[1]).slice(0, 8).map(([name]) => name);

// Stats — derive from real data
const totalProducts    = activeProducts.length;
const totalCategories  = activeCategories.length;
const totalSubcategories = (await Promise.all(activeCategories.map(c => storage.getSubcategoriesByCategoryId(c.id)))).flat().filter(s => s.isActive).length;

// Schema
const baseUrl = siteSettings.siteUrl || 'https://supplementskenya.com';
const homePageSchema = generateHomePageSchema();
const featuredListSchema = bestsellers.length > 0
  ? generateItemListSchema(
      bestsellers.map(p => ({ ...p, images: p.images || [], attributes: p.attributes || [] })),
      'Bestsellers',
      baseUrl
    )
  : null;
const structuredData = {
  "@context": "https://schema.org",
  "@graph": [...homePageSchema["@graph"], ...(featuredListSchema ? [featuredListSchema] : [])]
};

// Authorised stockist brands — the brands Supplements Kenya is an authorised distributor for
const STOCKIST_BRANDS = [
  'Life Extension',
  'Jarrow Formulas',
  'Vital Proteins',
  'Sports Research',
  'NeoCell',
  'Optimum Nutrition',
  'Nature Made',
];
---

<BaseLayout
  title={siteSettings.seo.defaultTitle}
  description={siteSettings.seo.defaultDescription}
  structuredData={structuredData}
>

  <!-- ─────────────────────── 1. HERO ─────────────────────── -->
  <section class="relative overflow-hidden">
    <div class="absolute inset-0 -z-10 bg-gradient-to-br from-primary/15 via-background to-accent/10"></div>
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-8 sm:py-10 md:py-14">
      <div class="grid grid-cols-1 lg:grid-cols-[2fr_1fr] gap-8 lg:gap-10 items-center">

        <!-- ── Left 70%: Hero content ── -->
        <div class="text-center lg:text-left">

          <h1 class="text-3xl sm:text-4xl md:text-5xl font-bold tracking-tight leading-[1.1] mb-5" data-testid="text-hero-title">
            {hero.title || (<>Hold your supplements to a <span class="text-primary">higher standard</span>.</>)}
          </h1>

          <!-- ── Compact review trust line — under H1 ── -->
          <div class="inline-flex items-center gap-3 mb-5">
            <div class="flex items-center gap-0.5">
              {[1,2,3,4,5].map(() => (
                <svg class="h-5 w-5 fill-amber-400 text-amber-400" viewBox="0 0 24 24"><path d="M12 .587l3.668 7.568L24 9.75l-6 5.847L19.336 24 12 19.897 4.664 24 6 15.597 0 9.75l8.332-1.595z"/></svg>
              ))}
            </div>
            <span class="text-base font-bold text-amber-700 dark:text-amber-300">4.8</span>
            <span class="text-sm text-muted-foreground">·&nbsp;1,200+ verified reviews in Kenya</span>
          </div>

          <p class="text-sm sm:text-base text-muted-foreground max-w-xl mb-5 mx-auto lg:mx-0">
            {hero.description || 'Clean ingredients. Honest labels. Clinically meaningful doses. Delivered across Kenya.'}
          </p>
          <div class="flex flex-col sm:flex-row items-center lg:items-start justify-center lg:justify-start gap-3 mb-6">
            <a href={hero.ctaLink || '/all-supplements/'}
               class="inline-flex items-center justify-center px-6 py-2.5 min-h-[44px] bg-primary text-primary-foreground font-semibold rounded-full hover:bg-primary/90 transition-colors w-full sm:w-auto"
               data-testid="button-hero-cta">
              {hero.ctaText || 'Shop Supplements'}
            </a>
            <a href="https://wa.me/254724410910" target="_blank" rel="noopener noreferrer"
               class="inline-flex items-center justify-center gap-2 px-6 py-2.5 min-h-[44px] font-semibold rounded-full border-2 border-[#25D366] text-[#25D366] hover:bg-[#25D366] hover:text-white transition-colors w-full sm:w-auto"
               data-testid="button-hero-whatsapp">
              <svg class="h-4 w-4 shrink-0" viewBox="0 0 24 24" fill="currentColor"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413Z"/></svg>
              Order on WhatsApp
            </a>
          </div>
          <!-- Goal-finder pills -->
          <div class="mb-6">
            <p class="text-xs text-muted-foreground mb-3 font-medium">I want to…</p>
            <div class="flex flex-wrap items-center justify-center lg:justify-start gap-2">
              {[
                { label: 'Build muscle',  href: '/protein/' },
                { label: 'Sleep better',  href: '/health-goals/' },
                { label: 'Boost energy',  href: '/specialty-supplements/' },
                { label: 'Look younger',  href: '/collagen/' },
                { label: 'Stay healthy',  href: '/vitamins/' },
                { label: 'Lose weight',   href: '/weight-management/' },
                { label: 'Think clearly', href: '/health-goals/' },
              ].map(g => (
                <a href={g.href}
                   class="px-4 py-1.5 rounded-full border bg-card text-sm font-medium hover:bg-primary hover:text-primary-foreground hover:border-primary transition-colors">
                  {g.label}
                </a>
              ))}
            </div>
          </div>
          <!-- Inline trust stats -->
          <div class="flex flex-wrap items-center justify-center lg:justify-start gap-x-6 gap-y-2 text-xs text-muted-foreground">
            <span class="flex items-center gap-1.5">
              <svg class="h-3.5 w-3.5 fill-amber-400 text-amber-400" viewBox="0 0 24 24"><path d="M12 .587l3.668 7.568L24 9.75l-6 5.847L19.336 24 12 19.897 4.664 24 6 15.597 0 9.75l8.332-1.595z"/></svg>
              <strong class="text-foreground">4.8★</strong>&nbsp;· 1,200+ verified reviews
            </span>
            <span class="hidden sm:flex items-center gap-1.5">
              <svg class="h-3.5 w-3.5 text-primary" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m5 12 5 5L20 7"/></svg>
              10,000+ orders delivered
            </span>
            <span class="flex items-center gap-1.5">
              <svg class="h-3.5 w-3.5 text-primary" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect width="16" height="13" x="6" y="4" rx="2"/><path d="M6 8H4a2 2 0 0 0-2 2v4a2 2 0 0 0 2 2h2"/><path d="M18 8h2a2 2 0 0 1 2 2v4a2 2 0 0 1-2 2h-2"/></svg>
              3-hr delivery in Nairobi
            </span>
          </div>
        </div>

        <!-- ── Right 30%: Bestseller card ── -->
        <div class="relative mx-auto w-full max-w-[320px] lg:max-w-none">
          <!-- Badge -->
          <div class="absolute -top-3 left-4 z-10 flex items-center gap-1.5 bg-amber-400 text-amber-950 text-xs font-bold px-3 py-1 rounded-full shadow-md">
            🏆 #1 Best Seller
          </div>
          <div class="bg-card border-2 border-primary/20 rounded-2xl overflow-hidden shadow-xl hover:shadow-2xl transition-shadow">
            <!-- Product image — single image, full cover, no overlap -->
            <a href="/collagen/collagen-peptides/vital-proteins-collagen-peptides-680g/" class="block relative overflow-hidden" style="aspect-ratio:4/3;">
              <img
                src="/images/vital-proteins-hero.webp"
                alt="Vital Proteins Collagen Peptides 680g — Buy in Kenya"
                class="absolute inset-0 w-full h-full object-cover hover:scale-105 transition-transform duration-500"
                loading="eager"
              />
            </a>
            <!-- Card body -->
            <div class="p-4">
              <p class="text-[11px] font-semibold uppercase tracking-wider text-primary mb-1">Vital Proteins</p>
              <h2 class="text-base font-bold leading-snug mb-1">
                <a href="/collagen/collagen-peptides/vital-proteins-collagen-peptides-680g/" class="hover:text-primary transition-colors">
                  Collagen Peptides 680g
                </a>
              </h2>
              <!-- Stars -->
              <div class="flex items-center gap-1 mb-2">
                {[1,2,3,4,5].map(() => (
                  <svg class="h-3.5 w-3.5 fill-amber-400 text-amber-400" viewBox="0 0 24 24"><path d="M12 .587l3.668 7.568L24 9.75l-6 5.847L19.336 24 12 19.897 4.664 24 6 15.597 0 9.75l8.332-1.595z"/></svg>
                ))}
                <span class="text-[11px] text-muted-foreground ml-1">4.9</span>
              </div>
              <p class="text-xl font-bold text-primary mb-3">KSh 8,995</p>
              <!-- CTAs -->
              <div class="flex flex-col gap-2">
                <a href="/collagen/collagen-peptides/vital-proteins-collagen-peptides-680g/"
                   class="inline-flex items-center justify-center px-4 py-2.5 min-h-[44px] bg-primary text-primary-foreground font-semibold rounded-full hover:bg-primary/90 transition-colors text-sm w-full">
                  Buy Now
                </a>
                <a href="https://wa.me/254724410910?text=Hi%2C+I%27d+like+to+order+Vital+Proteins+Collagen+Peptides+680g.+Product+link%3A+https%3A%2F%2Fsupplements.co.ke%2Fcollagen%2Fcollagen-peptides%2Fvital-proteins-collagen-peptides-680g%2F"
                   target="_blank" rel="noopener noreferrer"
                   class="inline-flex items-center justify-center gap-2 px-4 py-2.5 min-h-[44px] font-semibold rounded-full border-2 border-[#25D366] text-[#25D366] hover:bg-[#25D366] hover:text-white transition-colors text-sm w-full">
                  <svg class="h-4 w-4 shrink-0" viewBox="0 0 24 24" fill="currentColor"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413Z"/></svg>
                  WhatsApp Order
                </a>
              </div>
            </div>
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- ─────────────────── 2. TRUST BADGES STRIP ─────────────────── -->
  <section class="border-y bg-card">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-5">
      <div class="grid grid-cols-2 md:grid-cols-4 gap-6 md:gap-8">
        {[
          {
            t: 'Cash on delivery',
            d: 'Pay when your order arrives — no upfront risk',
            icon: '<path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75m-3-7.036A11.959 11.959 0 013.598 6 11.99 11.99 0 003 9.749c0 5.592 3.824 10.29 9 11.623 5.176-1.332 9-6.03 9-11.622 0-1.31-.21-2.571-.598-3.751h-.152c-3.196 0-6.1-1.248-8.25-3.285z"/>',
          },
          {
            t: 'Free delivery',
            d: 'Across all of Kenya — Nairobi deliveries within 3 hours',
            icon: '<path stroke-linecap="round" stroke-linejoin="round" d="M8.25 18.75a1.5 1.5 0 01-3 0m3 0a1.5 1.5 0 00-3 0m3 0h6m-9 0H3.375a1.125 1.125 0 01-1.125-1.125V14.25m17.25 4.5a1.5 1.5 0 01-3 0m3 0a1.5 1.5 0 00-3 0m3 0h1.125c.621 0 1.129-.504 1.09-1.124a17.902 17.902 0 00-3.213-9.193 2.056 2.056 0 00-1.58-.86H14.25M16.5 18.75h-2.25m0-11.177v-.958c0-.568-.422-1.048-.987-1.106a48.554 48.554 0 00-10.026 0 1.106 1.106 0 00-.987 1.106v7.635m12-6.677v6.677m0 4.5v-4.5m0 0h-12"/>',
          },
          {
            t: '30-day returns',
            d: 'Not satisfied? Return it within 30 days, no questions asked',
            icon: '<path stroke-linecap="round" stroke-linejoin="round" d="M16.023 9.348h4.992v-.001M2.985 19.644v-4.992m0 0h4.992m-4.993 0l3.181 3.183a8.25 8.25 0 0013.803-3.7M4.031 9.865a8.25 8.25 0 0113.803-3.7l3.181 3.182m0-4.991v4.99"/>',
          },
          {
            t: 'WhatsApp support',
            d: 'Message us anytime — real humans, real answers, fast',
            icon: '<path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413Z"/>',
            fill: true,
          },
        ].map(b => (
          <div class="flex flex-col items-center text-center md:items-start md:text-left gap-1">
            <svg class="h-7 w-7 text-primary mb-1" viewBox="0 0 24 24" fill={b.fill ? 'currentColor' : 'none'} stroke={b.fill ? 'none' : 'currentColor'} stroke-width="1.75">
              <Fragment set:html={b.icon} />
            </svg>
            <p class="text-sm font-semibold">{b.t}</p>
            <p class="text-xs text-muted-foreground leading-snug">{b.d}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ─────────── 2b. AUTHENTICITY SECTION ─────────── -->
  <section style="background-color:#f0faf4;" class="border-b">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-14">
      <div class="text-center mb-10">
        <p class="text-xs font-semibold tracking-wider uppercase text-primary mb-2">Why Kenyans trust us</p>
        <h2 class="text-2xl sm:text-3xl font-bold tracking-tight">If it's on this site, it's the real thing.</h2>
      </div>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-5">
        {[
          {
            icon: 'M9 12.75L11.25 15 15 9.75m-3-7.036A11.959 11.959 0 013.598 6 11.99 11.99 0 003 9.749c0 5.592 3.824 10.29 9 11.623 5.176-1.332 9-6.03 9-11.622 0-1.31-.21-2.571-.598-3.751h-.152c-3.196 0-6.1-1.248-8.25-3.285z',
            title: 'Direct from brand',
            desc: 'We source straight from manufacturers and authorised distributors. No grey-market middlemen, no switched product.',
          },
          {
            icon: 'M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4',
            title: 'Batch & expiry verified',
            desc: 'Every consignment is checked for batch numbers and expiry dates before it reaches our shelves — or your door.',
          },
          {
            icon: 'M20.25 14.15v4.25c0 1.094-.787 2.036-1.872 2.18-2.087.277-4.216.42-6.378.42s-4.291-.143-6.378-.42c-1.085-.144-1.872-1.086-1.872-2.18v-4.25m16.5 0a2.18 2.18 0 00.75-1.661V8.706c0-1.081-.768-2.015-1.837-2.175a48.114 48.114 0 00-3.413-.387m4.5 8.006c-.194.165-.42.295-.673.38A23.978 23.978 0 0112 15.75c-2.648 0-5.195-.429-7.577-1.22a2.016 2.016 0 01-.673-.38m0 0A2.18 2.18 0 013 12.489V8.706c0-1.081.768-2.015 1.837-2.175a48.111 48.111 0 013.413-.387m7.5 0V5.25A2.25 2.25 0 0013.5 3h-3a2.25 2.25 0 00-2.25 2.25v.894m7.5 0a48.667 48.667 0 00-7.5 0',
            title: 'KEBS-compliant import',
            desc: 'All products enter Kenya through formal import channels meeting Kenya Bureau of Standards requirements.',
          },
          {
            icon: 'M16.023 9.348h4.992v-.001M2.985 19.644v-4.992m0 0h4.992m-4.993 0l3.181 3.183a8.25 8.25 0 0013.803-3.7M4.031 9.865a8.25 8.25 0 0113.803-3.7l3.181 3.182m0-4.991v4.99',
            title: '30-day money-back',
            desc: 'If you are not satisfied for any reason, we refund you in full within 30 days. Simple, no hassle.',
          },
        ].map(card => (
          <div class="bg-white dark:bg-card rounded-xl p-5 flex flex-col gap-3 shadow-sm" style="border-left:4px solid var(--color-primary, #16a34a);">
            <div class="h-10 w-10 rounded-full bg-primary/10 flex items-center justify-center shrink-0">
              <svg class="h-5 w-5 text-primary" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75">
                <path stroke-linecap="round" stroke-linejoin="round" d={card.icon} />
              </svg>
            </div>
            <div>
              <p class="font-semibold text-sm mb-1">{card.title}</p>
              <p class="text-xs text-muted-foreground leading-relaxed">{card.desc}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ─────────── 3. CATEGORY NAVIGATION STRIP ─────────── -->
  <section class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 pt-6">
    <div class="flex flex-wrap items-center justify-center gap-x-2 gap-y-2 text-sm">
      {[
        { l: 'Bestsellers',          h: '/all-supplements/' },
        { l: 'Protein',              h: '/protein/' },
        { l: 'Build Muscle & Recover', h: '/protein/creatine/' },
        { l: 'Pre-Workout',          h: '/protein/pre-workout/' },
        { l: 'Health & Wellness',    h: '/health-goals/' },
        { l: 'Weight Management',    h: '/weight-management/' },
        { l: 'Vitamins',             h: '/vitamins/' },
        { l: 'Collagen',             h: '/collagen/' },
      ].map(c => (
        <a href={c.h}
           class="px-4 py-2 rounded-full border bg-card hover:bg-primary hover:text-primary-foreground hover:border-primary transition-colors font-medium">
          {c.l}
        </a>
      ))}
    </div>
  </section>

  <!-- ─────────────────── 4. BESTSELLERS ─────────────────── -->
  <section class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 pt-6 pb-14">
    <div class="flex items-end justify-between mb-4">
      <div>
        <p class="text-xs font-semibold tracking-wider uppercase text-primary mb-1">Most popular</p>
        <h2 class="text-2xl sm:text-3xl font-bold tracking-tight">Bestsellers</h2>
      </div>
      <a href="/all-supplements/" class="hidden sm:inline-flex items-center text-sm font-semibold text-primary hover:underline">
        View all →
      </a>
    </div>
    {bestsellers.length > 0 ? (
      <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 gap-4 sm:gap-6">
        {bestsellers.slice(0, 10).map(p => (
          <ProductCard product={{ ...p, images: p.images || [], attributes: p.attributes || [], subcategorySlug: p.subcategorySlug || '' }} />
        ))}
      </div>
    ) : (
      <p class="text-muted-foreground text-sm">Bestsellers will appear here once products are loaded.</p>
    )}
  </section>

  <!-- ─────────── 5. FIND YOUR CLEAN SUPPLEMENTS ─────────── -->
  <section class="bg-muted/30 border-y">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-14">
      <div class="text-center max-w-2xl mx-auto mb-10">
        <p class="text-xs font-semibold tracking-wider uppercase text-primary mb-1">By supplement type</p>
        <h2 class="text-2xl sm:text-3xl font-bold tracking-tight">Find your clean supplements</h2>
        <p class="text-sm sm:text-base text-muted-foreground mt-2">Shop by what your body actually needs, not by hype.</p>
      </div>

      <div class="space-y-12">
        {supplementTypes.map(group => (
          <div>
            <div class="flex items-baseline justify-between mb-4">
              <h3 class="text-xl sm:text-2xl font-bold">{group.name}</h3>
              <a href={`/${group.slug}/`} class="text-sm font-semibold text-primary hover:underline">Shop all {group.name.toLowerCase()} →</a>
            </div>
            <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-6 gap-4">
              {group.products.map(p => (
                <ProductCard product={{ ...p, images: p.images || [], attributes: p.attributes || [], subcategorySlug: p.subcategorySlug || '' }} />
              ))}
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ─────────────── 6. SHOP BY GOAL (2x2) ─────────────── -->
  <section class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-14">
    <div class="text-center max-w-2xl mx-auto mb-10">
      <p class="text-xs font-semibold tracking-wider uppercase text-primary mb-1">Goal-based</p>
      <h2 class="text-2xl sm:text-3xl font-bold tracking-tight">Shop by goal</h2>
    </div>
    <div class="grid grid-cols-1 sm:grid-cols-2 gap-5">
      {goalTiles.map((t, i) => {
        const tints = [
          'from-emerald-700 to-emerald-900',
          'from-rose-700 to-rose-900',
          'from-blue-700 to-blue-900',
          'from-amber-700 to-amber-900',
        ];
        return (
          <a href={`/${t.cat!.slug}/`}
             class={`group relative overflow-hidden rounded-2xl bg-gradient-to-br ${tints[i % tints.length]} p-8 sm:p-10 min-h-[220px] sm:min-h-[280px] flex flex-col justify-end text-white hover:shadow-2xl transition-all`}
             style={t.cat!.heroImage ? `background-image: linear-gradient(to bottom right, rgba(0,0,0,0.55), rgba(0,0,0,0.75)), url('${t.cat!.heroImage}'); background-size: cover; background-position: center;` : ''}>
            <h3 class="text-2xl sm:text-3xl font-bold mb-2 leading-tight">{t.label}</h3>
            <p class="text-sm sm:text-base text-white/85 mb-4">{t.blurb}</p>
            <span class="inline-flex items-center text-sm font-semibold border-b border-white/40 pb-1 self-start group-hover:border-white transition-colors">
              Shop {t.label.toLowerCase()} →
            </span>
          </a>
        );
      })}
    </div>
  </section>

  <!-- ─────────────── 7. AUTHORISED STOCKIST BRAND STRIP ─────────────── -->
  <section class="border-y bg-muted/40">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-10">
      <p class="text-xs font-semibold tracking-wider uppercase text-muted-foreground text-center mb-6">
        Authorised Kenyan distributor for
      </p>
      <div class="flex flex-wrap items-center justify-center gap-x-8 gap-y-4 sm:gap-x-12">
        {STOCKIST_BRANDS.map(brand => (
          <span class="text-base sm:text-lg font-bold text-foreground/70 tracking-tight whitespace-nowrap">
            {brand}
          </span>
        ))}
      </div>
    </div>
  </section>

  <!-- ─────────────── 9. STATS ROW ─────────────── -->
  <section class="bg-primary text-primary-foreground">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-14">
      <div class="text-center mb-10">
        <p class="text-xs font-semibold tracking-wider uppercase opacity-80 mb-2">Why Kenyans choose us</p>
        <h2 class="text-2xl sm:text-3xl font-bold tracking-tight">Your supplement order, done right</h2>
      </div>
      <div class="grid grid-cols-2 md:grid-cols-4 gap-6 md:gap-4 text-center">
        {[
          { v: `${totalProducts}+`, l: 'Products in stock' },
          { v: '5,000+',            l: 'Orders shipped across Kenya' },
          { v: '4.8★',             l: 'Average customer rating' },
          { v: '3 hrs',             l: 'Delivery within Nairobi' },
        ].map(s => (
          <div>
            <div class="text-3xl sm:text-4xl font-bold mb-1">{s.v}</div>
            <div class="text-xs sm:text-sm opacity-85">{s.l}</div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ─────────────── 9b. TESTIMONIALS ─────────────── -->
  <section class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-14">
    <div class="text-center mb-10">
      <p class="text-xs font-semibold tracking-wider uppercase text-primary mb-2">Customer reviews</p>
      <h2 class="text-2xl sm:text-3xl font-bold tracking-tight">What our customers say</h2>
    </div>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
      {[
        {
          name: 'Mary Wanjiku',
          city: 'Nairobi',
          stars: 5,
          product: 'Optimum Nutrition Whey',
          date: 'February 2026',
          review: 'I have tried several protein powders sold in Kenya and most turned out to be fake. The ON Whey from here mixed well, tasted right, and had the proper seal. You can tell straight away it is genuine. Fast delivery to South C as well.',
        },
        {
          name: 'Daniel Omondi',
          city: 'Mombasa',
          stars: 5,
          product: 'Life Extension Omega-3',
          date: 'March 2026',
          review: "Ordered the Life Extension fish oil after a friend recommended the site. Arrived in Mombasa within two days. No fishy aftertaste like the ones I bought elsewhere — which tells me it is properly stored and fresh. Will keep reordering.",
        },
        {
          name: 'Aisha Mohammed',
          city: 'Kisumu',
          stars: 4,
          product: 'Neocell Marine Collagen',
          date: 'January 2026',
          review: 'Good quality collagen and packaging was intact. Delivery took slightly longer to Kisumu than I expected but customer support on WhatsApp was helpful and kept me updated. Product itself is excellent — skin looks noticeably better after six weeks.',
        },
      ].map(t => (
        <div class="bg-card border rounded-xl p-6 flex flex-col gap-4">
          <!-- Stars -->
          <div class="flex items-center gap-0.5">
            {[1,2,3,4,5].map(star => (
              <svg class={`h-4 w-4 ${star <= t.stars ? 'fill-amber-400 text-amber-400' : 'fill-gray-200 text-gray-200 dark:fill-gray-700 dark:text-gray-700'}`} viewBox="0 0 24 24"><path d="M12 .587l3.668 7.568L24 9.75l-6 5.847L19.336 24 12 19.897 4.664 24 6 15.597 0 9.75l8.332-1.595z"/></svg>
            ))}
          </div>
          <!-- Review text -->
          <p class="text-sm text-foreground/80 leading-relaxed flex-1">"{t.review}"</p>
          <!-- Product tag -->
          <p class="text-xs text-primary font-medium">Purchased: {t.product}</p>
          <!-- Reviewer -->
          <div class="border-t pt-3 flex items-center justify-between">
            <div>
              <p class="text-sm font-semibold">{t.name}</p>
              <p class="text-xs text-muted-foreground">{t.city}</p>
            </div>
            <p class="text-xs text-muted-foreground">{t.date}</p>
          </div>
        </div>
      ))}
    </div>
  </section>

  <!-- ─────────────── 10. BRAND STRIP ─────────────── -->
  {topBrands.length > 0 && (
    <section class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-12 border-b">
      <p class="text-xs font-semibold tracking-wider uppercase text-muted-foreground text-center mb-6">Trusted brands we stock</p>
      <div class="flex flex-wrap items-center justify-center gap-x-8 gap-y-4 sm:gap-x-12">
        {topBrands.map(b => (
          <a
            href={`/brand/${b.toLowerCase().replace(/[^a-z0-9\s-]/g, '').replace(/\s+/g, '-')}/`}
            class="text-base sm:text-lg font-bold text-foreground/70 hover:text-primary tracking-tight whitespace-nowrap transition-colors"
            data-testid={`link-brand-${b.toLowerCase().replace(/[^a-z0-9\s-]/g, '').replace(/\s+/g, '-')}`}
          >
            {b}
          </a>
        ))}
      </div>
    </section>
  )}

  <!-- ─────────────── 11. EVERY INGREDIENT MATTERS CTA ─────────────── -->
  <section class="mx-auto max-w-4xl px-4 sm:px-6 lg:px-8 py-16 text-center">
    <h2 class="text-3xl sm:text-4xl font-bold tracking-tight mb-4">
      Every ingredient matters.
    </h2>
    <p class="text-base sm:text-lg text-muted-foreground mb-7 max-w-2xl mx-auto">
      No proprietary blends. No hidden fillers. Just honest, evidence-backed nutrition.
    </p>
    <a href="/all-supplements/"
       class="inline-flex items-center justify-center px-7 py-3.5 min-h-[48px] bg-primary text-primary-foreground font-semibold rounded-full hover:bg-primary/90 transition-colors">
      Shop bestsellers
    </a>
  </section>

  <!-- ─────────────── 12. SEO / EDUCATIONAL CONTENT ─────────────── -->
  <section class="bg-muted/30 border-y">
    <div class="mx-auto max-w-4xl px-4 sm:px-6 lg:px-8 py-14">
      <div class="text-center mb-6">
        <p class="text-xs font-semibold tracking-wider uppercase text-primary mb-2">Start learning</p>
        <h2 class="text-2xl sm:text-3xl font-bold tracking-tight">
          {seoContent.title || 'Your trusted supplement store in Kenya'}
        </h2>
      </div>
      <div class="prose prose-sm sm:prose-base dark:prose-invert max-w-none text-muted-foreground">
        {seoContent.content ? (
          <Fragment set:html={seoContent.content} />
        ) : (
          <>
            <p>
              Welcome to Supplements Kenya. We curate vitamins, proteins, collagen, omega-3s, probiotics, and
              wellness products from globally trusted brands, then verify authenticity before they reach you.
            </p>
            <p>
              Whether you're building a daily wellness routine or chasing a specific goal, our catalogue is
              organised by what you actually want to achieve, not by clever marketing buckets. Every product
              ships across Kenya with reliable, fast delivery.
            </p>
          </>
        )}
      </div>
    </div>
  </section>

  <!-- ─────────────── 13. NEWSLETTER ─────────────── -->
  <section class="mx-auto max-w-3xl px-4 sm:px-6 lg:px-8 py-16 text-center">
    <h2 class="text-2xl sm:text-3xl font-bold tracking-tight mb-3">Get 10% off your first order</h2>
    <p class="text-sm sm:text-base text-muted-foreground mb-6">
      Join the newsletter for evidence-based supplement advice, restock alerts, and Kenya-only offers.
    </p>
    <form class="flex flex-col sm:flex-row gap-2 sm:gap-3 max-w-lg mx-auto" data-testid="form-newsletter">
      <input type="text" placeholder="Your name" required
             class="flex-1 px-4 py-3 min-h-[44px] border rounded-full bg-background focus:outline-none focus:ring-2 focus:ring-primary/30"
             data-testid="input-newsletter-name" />
      <input type="email" placeholder="you@example.com" required
             class="flex-1 px-4 py-3 min-h-[44px] border rounded-full bg-background focus:outline-none focus:ring-2 focus:ring-primary/30"
             data-testid="input-newsletter-email" />
      <button type="submit"
              class="inline-flex items-center justify-center px-6 py-3 min-h-[44px] bg-primary text-primary-foreground font-semibold rounded-full hover:bg-primary/90 transition-colors"
              data-testid="button-newsletter-submit">
        Sign up
      </button>
    </form>
    <p class="text-xs text-muted-foreground mt-3">No spam. Unsubscribe anytime.</p>
  </section>

</BaseLayout>
